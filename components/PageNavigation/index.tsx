import { Wrap } from "../styled";

function PageNavigation({}): JSX.Element {
  return (
    <Wrap display={["block", "none"]}>
      <h2>Page Navigation</h2>
    </Wrap>
  );
};

export default PageNavigation; 